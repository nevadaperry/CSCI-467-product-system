import React from 'react';
import { Button } from 'reactstrap';

function OrderList() {
	
	return (
		<p> TODO: make this just like the admin order listing, but with a button that selects the order and switches to the "Documents" tab </p>
	);
}

export default OrderList;